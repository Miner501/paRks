% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigate_to_target.R
\name{navigate_to_target}
\alias{navigate_to_target}
\title{Navigate to a green or blue space based on user preference}
\usage{
navigate_to_target(
  location,
  zone,
  greens,
  blues,
  target_type = "green",
  preference = "closest"
)
}
\arguments{
\item{location}{A tibble with \code{lat} and \code{lon} columns (from geocoding)}

\item{zone}{An \code{sf} POLYGON representing the travel zone}

\item{greens}{An \code{sf} object of green space geometries (e.g. parks)}

\item{blues}{An \code{sf} object of blue space geometries (e.g. rivers)}

\item{target_type}{One of \code{"green"} or \code{"blue"} to select destination type}

\item{preference}{One of \code{"closest"} or \code{"largest"} to define selection criteria}
}
\value{
A list with two elements:
\describe{
\item{route}{An \code{sf} LINESTRING object representing the road network route}
\item{length_m}{A numeric value giving the route length in meters}
}
}
\description{
Prioritizes either the closest or largest destination within a given travel zone,
calculates the route to it, and returns both the route and its travel distance.
}
\examples{
location <- geocode_address("Kiliansplatz, Wuerzburg")
zone <- travel_zone(location, 5)
\dontrun{
# assuming you have loaded `greens` and `blues` as sf objects
result <- navigate_to_target(location, zone, greens, blues, target_type = "green", preference = "closest")
}

}
