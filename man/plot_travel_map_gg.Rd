% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapping.R
\name{plot_travel_map_gg}
\alias{plot_travel_map_gg}
\title{Plot travel map using ggplot2}
\usage{
plot_travel_map_gg(
  zone,
  greens = NULL,
  blues = NULL,
  roads = NULL,
  route = NULL,
  route_distance = NULL,
  start_location = NULL,
  show_greens = TRUE,
  show_blues = TRUE,
  show_roads = TRUE,
  show_route = TRUE,
  title = "Travel Zone Map (ggplot2)",
  palette = "normal",
  target_type = NULL
)
}
\arguments{
\item{zone}{An \code{sf} POLYGON object representing the travel zone.}

\item{greens}{Optional \code{sf} object of green space polygons (parks, gardens, etc.).}

\item{blues}{Optional \code{sf} object of blue space polygons or lines (rivers, lakes, etc.).}

\item{roads}{Optional \code{sf} object of road geometries (LINESTRING).}

\item{route}{Optional \code{sf} LINESTRING representing the route to a target.}

\item{route_distance}{Numeric. Distance in meters to the destination, used in caption.}

\item{start_location}{A tibble with \code{lat} and \code{lon} columns representing the geocoded start point.}

\item{show_greens}{Logical. If \code{TRUE}, green spaces are added to the map. Default is \code{TRUE}.}

\item{show_blues}{Logical. If \code{TRUE}, blue spaces are added to the map. Default is \code{TRUE}.}

\item{show_roads}{Logical. If \code{TRUE}, roads are added to the map. Default is \code{TRUE}.}

\item{show_route}{Logical. If \code{TRUE}, the route is added to the map. Default is \code{TRUE}.}

\item{title}{Title to be displayed at the top of the plot. Default is \code{"Travel Zone Map (ggplot2)"}.}

\item{palette}{Name of the color palette to use. Options: \code{"normal"}, \code{"deuteranopia"}, \code{"protanopia"}, \code{"tritanopia"}.}

\item{target_type}{String. Either "green" or "blue", describing type of target shown in caption.}
}
\value{
A ggplot object showing the map with specified features.
}
\description{
Replaces base R plotting with ggplot2 for better styling and customization.
Allows for visualizing green/blue spaces, roads, routes, and the travel zone itself.
}
\examples{
location <- geocode_address("Kiliansplatz, Wuerzburg")
zone <- travel_zone(location, 5)
\dontrun{
result <- navigate_to_target(location, zone, greens, blues)
plot_travel_map_gg(
  zone = zone,
  greens = greens,
  blues = blues,
  roads = roads,
  route = result$route,
  route_distance = result$length_m,
  start_location = location,
  target_type = "green"
)
}


}
